sudo: required

services: 
  - docker

before_install: 
  - docker build -t kyrie/react-test -f ./client/Dockerfile.dev ./client

script: 
  - docker run kyrie/react-test npm test -- --coverage

after_success: 
  - docker build -t kyrie/multi-client ./client
  - docker build -t kyrie/multi-nginx ./nginx
  - docker build -t kyrie/multi-server ./server
  - docker build -t kyrie/multi-worker ./worker

#log us in to docker CLI
- echo "$DOCKER_PW" | docker login -u "$DOCKER_ID" --password-stdin

# Take those images and push them to docker hub
- docker push kyrie/multi-client
- docker push kyrie/multi-nginx
- docker push kyrie/multi-server
- docker push kyrie/multi-worker
